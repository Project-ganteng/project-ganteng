import React from 'react';
import Dashboard from './Dashboard';

// This is an alias for Dashboard component to maintain backward compatibility
const RoomDashboard = () => {
  return <Dashboard />;
};

export default RoomDashboard;
